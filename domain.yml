version: "3.1"

intents:
  - hide_module
  - show_module
  - handle_to_ai:
      # This tells Rasa we dont wanna have any entities when this is the intent
      use_entities: []

entities:
  - module
