version: "3.1"

intents:
  - hide_module
  - show_module
  - handle_to_ai:
      # This tells Rasa we dont wanna have any entities when this is the intent
      use_entities: []
  - handle_to_ai_with_pic

entities:
  - module
